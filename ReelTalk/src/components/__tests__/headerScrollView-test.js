jest.dontMock('../HeaderScrollView.jsx');

const HeaderScrollView = require('../HeaderScrollView.jsx');

describe('HeaderScrollView', () => {
  it('fires click event', () => {
    expect(1).toBe(1)
  });

});
